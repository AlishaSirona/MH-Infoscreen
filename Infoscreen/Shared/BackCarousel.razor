@inject ImgData ImgData

@foreach (var img in ImgData.BackList.OrderBy(item => item.Position))
{
    <MudCarouselItem Transition="Transition.Fade">
        <img src="@img.InternFilePath" />
    </MudCarouselItem>
}